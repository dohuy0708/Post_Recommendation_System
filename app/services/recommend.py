
def get_recommendations(user_id: int):
    try:
        # Giả sử bạn sử dụng một model gợi ý hoặc logic gì đó
        recommendations = ["Naruto", "Attack on Titan"]  # Dữ liệu giả lập
        return recommendations
    except Exception as e:
        raise Exception(f"Error in recommendation logic: {str(e)}")
